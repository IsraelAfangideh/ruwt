# Privacy

Ruwt disables raw prompts and raw source code collection. It stores event
metadata only after redaction. Metadata keys that name credentials, tokens,
cookies, passwords, or secrets become `[REDACTED]` before sync.

The intelligence view reports confidence and coverage. It does not rank
engineers. Administrators must create an organization before data collection.
Local users can pause collection and export or remove local queue data.
