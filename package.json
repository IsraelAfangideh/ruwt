{
  "name": "ruwt",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev:dev": "npm --prefix dev run dev",
    "dev:health": "npm --prefix health run dev",
    "build:dev": "npm --prefix dev run build",
    "build:health": "npm --prefix health run build",
    "test:dev": "npm --prefix dev run test",
    "lint:dev": "npm --prefix dev run lint",
    "lint:health": "npm --prefix health run lint",
    "lint": "npm --prefix dev run lint && npm --prefix health run lint",
    "test": "npm --prefix dev run test",
    "build": "npm --prefix dev run build && npm --prefix health run build",
    "typecheck": "npm --prefix dev run typecheck && npm --prefix health run typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
