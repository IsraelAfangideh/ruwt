{
  "name": "@ruwt/web",
  "version": "1.0.0",
  "description": "Web app for Ruwt - built from mobile codebase using Expo Web",
  "scripts": {
    "dev": "cd ../mobile && npx expo start --web",
    "build:expo": "cd ../mobile && npx expo export --platform web",
    "build:copy": "node scripts/copy-static.js",
    "build": "npm run build:expo && npm run build:copy",
    "serve": "npx serve dist"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}

