name = "ruwt-dev"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

[[d1_databases]]
binding = "DB"
database_name = "ruwt-dev"
database_id = "27b64c12-c858-473d-8a40-d202d01d32aa"
# After creating: wrangler d1 create ruwt-dev
# Then: wrangler d1 execute ruwt-dev --remote --file=./drizzle/migrations-d1/0000_initial.sql

# Public (non-secret) vars needed by Pages Functions at runtime.
# Secrets (Stripe keys, API tokens) must be set in dashboard or via
# `npx wrangler pages secret put <NAME>`.
[vars]
VITE_SUPABASE_URL = "https://fzncpdelyfuvdeqmwznx.supabase.co"
VITE_SUPABASE_ANON_KEY = "sb_publishable_tjjTFEzLtcwHrrS9qpkhdQ_rfGJHIsy"
