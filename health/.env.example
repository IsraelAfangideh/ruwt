# Client / build (Vite) – used in the browser
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Cloudflare Pages / Functions: set in dashboard or .dev.vars (not committed)
# D1 is bound in wrangler.toml (DB). No DATABASE_URL.
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
